name: telegram-bot
type: application
region: frankfurt # Change if needed

language: python
version: "3.11"

build:
  type: python
  command: pip install -r requirements.txt # Ensure dependencies are installed

run:
  command: python main.py # Adjust based on your bot’s entry point

ports:
  - port: 8000
    protocol: http # Change to HTTP if using Flask for health check

healthcheck:
  type: http
  path: /health
  interval: 10s
  timeout: 5s
  retries: 3

env:
  - name: BOT_TOKEN
    value: "7568918921:AAHT6_qx76-7hyG74S2T0eG1R1yWgqzXmWo"
  - name: FLASK_PORT
    value: "8000"
